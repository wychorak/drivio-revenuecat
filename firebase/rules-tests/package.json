{
  "name": "drivio-firestore-rules-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test firestore.rules.test.mjs",
    "test:emulator": "firebase emulators:exec --config ../../firebase.json --project demo-drivio-rules --only firestore \"npm test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.16.0",
    "firebase-tools": "15.25.0"
  }
}
